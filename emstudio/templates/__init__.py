# SPDX-License-Identifier: LGPL-2.1-or-later
"""EMStudio templates: the ready-to-solve geometries the Templates menu builds.

Eighteen modules, one per template, each exposing the ``makeX()`` that both the
menu command and ``tools/gen_examples.py`` call — so the shipped example
documents come from the same entry point a user does.
"""
